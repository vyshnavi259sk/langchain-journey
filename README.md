Phase 1 is the basics: Day 1-5  
Day 1 and 2: JSON  
To parse an API response in JSON into a python dictionary and to send a dictionary as a JSON to an API.  
Day 3: HTTP GET  
To get a response to a question without sending data (GET).  
Day 4: AI API  
To create .env to store API keys and to talk to an AI API by sending a prompt (POST). AI has no memory so we simulate it manually.  
Day 5: venv  
To create virtual environments so that no conflicts between projects./

Phase 2 is about LangChain: Day 6-10  
Day 6: LangChain  
The entire prompt is reduced to one line and now the AI memory is automated. Groq is used to connect to llama-3.1-8b-instant model.  
Day 7: Templates  
Prompt templates are reusable for different inputs using {} to get filled at runtime.  
Chain -> Prompt | LLM  
Day 8: Chains  
8.1 :   
Load API key  
LLM defined with llama as model and groq as provider  
Prompt defined  
Parser defined  
Chain -> Prompt1 | LLM | Parser  
Response generated by invoking the chain with an input question  
8.2 :  
Steps same as 8.1 except Chain -> Prompt1 | Prompt2 | ... | LLM | Parser  
Day 9
